<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Random <%= type.charAt(0).toUpperCase() + type.slice(1) %></title>
    <style>
   body {
    font-family: Arial, sans-serif;
    background-color: teal;
    margin: 0;
    padding: 20px; /* Added padding to the body */
}

.container {
    max-width: 800px;
    margin: 0 auto; /* Center the container horizontally */
    padding: 20px;
    background-color: white;
    border-radius: 8px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    text-align: center;
}

h1 {
    margin-bottom: 20px;
    font-size: 24px;
    color: #333;
}

.content {
    display: flex;
    flex-direction: column;
    align-items: center;
}

img {
    max-width: 300px;
    border-radius: 8px;
    margin-bottom: 20px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
}

.details {
    text-align: center;
}

.details h2 {
    margin: 10px 0;
    font-size: 20px;
    color: #555;
}

.details p {
    font-size: 16px;
    color: #777;
    line-height: 1.6;
    margin-bottom: 20px;
}

.next-button {
    background-color: #007bff;
    color: white;
    border: none;
    border-radius: 4px;
    padding: 12px 20px;
    font-size: 16px;
    cursor: pointer;
    transition: background-color 0.3s ease;
    margin-top: 20px;
}

.next-button:hover {
    background-color: #0056b3;
}

       
    </style>
</head>
<body>
    <div class="final">
    <div class="container">
        <h1>Random <%= type.charAt(0).toUpperCase() + type.slice(1) %></h1>
        <div class="content">
            <img src="<%= data.images.jpg.large_image_url %>" alt="<%= data.title %>">
            <div class="details">
                <h2>JP Title: <%= data.title %></h2>
                <h2>ENG Title: <%= data.title_english %></h2>
                <% if (type === 'manga') { %>
                    <h2>Vol: <%= data.volumes %> Chap: <%= data.chapters %></h2>
                <% } %>
                <% if (type === 'anime') { %>
                    <h2>Episodes: <%= data.episodes %> Type: <%= data.type %></h2>
                <% } %>
                <h2>Status: <%= data.status %></h2>
                <p><%= data.synopsis %></p>
            </div>
            <form action="/random" method="get">
                <input type="hidden" name="type" value="<%= type %>">
                <button type="submit" class="next-button">Next</button>
            </form>
        </div>
    </div>
    </div>
    
    <p>&#169; Developed by SanthanaKrishnan V</p>
</body>
</html>
